
&НаСервере
Процедура НайтиКоординатыНаСервере()
	
	Координаты = ГАЛ_ApiYandex.YandexGeoCode(Запись.Адрес);
	
	Если ТипЗнч(Координаты) = Тип("Структура") Тогда	
			Запись.Широта  = Координаты.lat;
			Запись.Долгота = Координаты.lng;
   КонецЕсли;


  
КонецПроцедуры

&НаКлиенте
Процедура НайтиКоординаты(Команда)
	НайтиКоординатыНаСервере();
КонецПроцедуры
